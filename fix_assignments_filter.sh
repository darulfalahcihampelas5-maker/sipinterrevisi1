sed -i 's/return assign.targets.some((t: any) => t.kelas === student.kelas);/return assign.targets.some((t: any) => (t.kelas || "").toString().trim().toLowerCase() === (student.kelas || "").toString().trim().toLowerCase());/g' src/pages/DashboardStudent.tsx
sed -i 's/return assign.kelas === student.kelas || !assign.kelas;/const aKelas = (assign.kelas || "").toString().trim().toLowerCase(); const sKelas = (student.kelas || "").toString().trim().toLowerCase(); return aKelas === sKelas || !aKelas || aKelas === "semua kelas" || aKelas === "semua_kelas";/g' src/pages/DashboardStudent.tsx
