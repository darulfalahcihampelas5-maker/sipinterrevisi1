sed -i 's/const studentKelas = student?.kelas || "";/const studentKelas = (student?.kelas || "").toString().trim().toLowerCase();/g' src/pages/DashboardStudent.tsx
sed -i 's/return rawExams.filter((exam: any) => !exam.kelasRef || exam.kelasRef === "SEMUA_KELAS" || exam.kelasRef === "" || exam.kelasRef === "Semua Kelas" || exam.kelasRef === studentKelas);/return rawExams.filter((exam: any) => { const ref = (exam.kelasRef || "").toString().trim().toLowerCase(); return !ref || ref === "semua_kelas" || ref === "semua kelas" || ref === studentKelas; });/g' src/pages/DashboardStudent.tsx
